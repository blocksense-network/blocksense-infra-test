import React from 'react';

import { DataFeeds } from '@/components/DataFeeds/DataFeeds';
export { getStaticProps } from '@/components/DataFeeds/DataFeeds';

export default function FeedsPage({ feedsConfig }) {
    return <DataFeeds feedsConfig={feedsConfig} />;
}
